DATABASE_URL="postgresql://user:password@localhost:5432/ong_platform"
JWT_SECRET="change-me-in-production"
PORT=4000
PUBLIC_APP_URL="https://app.exemple.org"

# SMTP (envoi des bulletins de paie et factures par email).
# Laisser vide en développement : les envois sont alors simulés et tracés en log.
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="no-reply@plateforme-ong.org"
